name: Run EF Migrations

on:
  workflow_dispatch:
    inputs:
      resource_group:
        description: 'Resource Group'
        required: true
        default: 'rg-karamel-prod'
      sql_server_name:
        description: 'SQL Server name'
        required: true
        default: 'rg-karamel-prod-sqlsrv'
      sql_database_name:
        description: 'SQL Database name'
        required: true
        default: 'rg-karamel-prod-sqldb'
      project_path:
        description: 'Path to the backend project'
        required: true
        default: 'Karamel.Backend'

jobs:
  run-migrations:
    runs-on: [self-hosted, linux, rg-karamel-prod-runner]
    steps:
      - uses: actions/checkout@v4

      - name: Login to Azure (VM Managed Identity)
        run: az login --identity

      - name: Install dotnet SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install EF Core tools
        run: dotnet tool install --global dotnet-ef

      - name: Get access token for SQL
        id: get-token
        run: |
          TOKEN=$(az account get-access-token --resource https://database.windows.net/ --query accessToken -o tsv)
          echo "::add-mask::$TOKEN"
          echo "SQL_ACCESS_TOKEN=$TOKEN" >> $GITHUB_ENV

      - name: Run migrations (AAD auth via Private Endpoint)
        run: |
          # Build connection string with AAD token (using private endpoint)
          # Note: SQL Server is configured with public access disabled, so this uses the private endpoint
          export ConnectionStrings__DefaultConnection="Server=tcp:${{ github.event.inputs.sql_server_name }}.database.windows.net,1433;Database=${{ github.event.inputs.sql_database_name }};Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
          
          echo "Connecting via private endpoint with managed identity authentication..."
          
          # Run migrations (EF Core will use DefaultAzureCredential from VM's managed identity)
          cd ${{ github.event.inputs.project_path }}
          dotnet ef database update --no-build || {
            echo "Migration failed! Check that:"
            echo "1. VM has network connectivity to SQL Server private endpoint"
            echo "2. VM's managed identity has appropriate SQL permissions"
            exit 1
          }
        env:
          AZURE_ACCESS_TOKEN: ${{ env.SQL_ACCESS_TOKEN }}